import Vue from 'vue'
import Vuex, { Store } from 'vuex'
import * as modules from './modules'

Vue.use(Vuex);

export default new Store({
  modules,
  state: {
  },
  getters: {
  },
  actions: {
  },
  mutations: {
  },
});
